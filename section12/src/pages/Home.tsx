// import { useSearchParams } from "react-router-dom";

const Home = () => {
    // 쿼리스트링 가져오기 (수정 시 setParams 사용)
    // const [params] = useSearchParams();
    // const paramValue: string | null = params.get('paramName');

    return <div>Home</div>
}

export default Home;